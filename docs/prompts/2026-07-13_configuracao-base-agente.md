# Registro de Prompt

**Data:** 2026-07-13
**Objetivo da Tarefa:** Configurar a base do projeto para o agente em Python utilizando LangGraph, incluindo a criação dos arquivos iniciais de dependências, fluxo e ambiente.

## Prompt Exato:

```text
Você é um desenvolvedor especialista em agentes de IA.

Precisamos integrar um agente a este projeto que faça a consulta de eventos através de uma chamada à API no endpoint /api/events para listar os eventos para o usuário.

Para isso, criei uma pasta da raíz do projeto, onde implementaremos o agente em python, usando LangGraph.

Os requisitos da entrega estão no arquivo agent/projetoAvaliativo/projetoAvaliativo-modulo2.md.

Execute as seguinte etapas:

# Cria o arquivo de dependências
touch requirements.txt

# Cria o arquivo principal do fluxo do agente
touch main.py

# Cria os arquivos de variáveis de ambiente
touch .env .env.example
```
