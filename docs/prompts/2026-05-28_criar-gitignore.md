# Prompt Log — Criar .gitignore na raiz do projeto

## Data
2026-05-28

## Prompt Enviado pelo Usuário

> crie um arquivo .gitignore na raíz do projeto para não subir arquivos e pastas que comumente não sobem em projetos, como a pasta node_modules.

## Objetivo da Tarefa

Criar um arquivo `.gitignore` na raiz do projeto cobrindo padrões comuns para projetos Node.js/React (backend + frontend), incluindo `node_modules`, builds compilados, arquivos de ambiente, logs e arquivos de sistema operacional.
